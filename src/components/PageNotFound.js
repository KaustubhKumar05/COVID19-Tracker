import { Link } from "react-router-dom";

const PageNotFound = () => {
	return (
		<div>
			<Link to="/">Page does not exist! Return home</Link>
		</div>
	);
};

export default PageNotFound;
